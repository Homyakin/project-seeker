itemWithoutModifiers = "${item}"
itemWithPrefixModifier = "${prefix_modifier} ${item}"
itemWithSuffixModifier = "${item} ${suffix_modifier}"
itemWithPrefixAndSuffixModifier = "${prefix_modifier} ${item} ${suffix_modifier}"
itemWithTwoPrefixModifiers = "${prefix_modifier_one} ${prefix_modifier_two} ${item}"