# Поэтапная переработка экипировки, походов и аномалий

Обновлено: 2026-09-12
Текущий выполняемый шаг: **нет; следующий по плану — шаг 7**

## Правила выполнения

- Статусы: `[ ]` — не начат, `[~]` — выполняется, `[x]` — завершён, `[!]` — заблокирован.
- За одну команду выполняется ровно один пронумерованный шаг.
- Переход к следующему шагу — только по явной команде пользователя.
- Перед началом шага его статус меняется на `[~]`.
- После завершения шага в этот файл добавляются результаты, принятые решения, проверки и обнаруженные риски.
- Описание игрового решения сначала утверждается, а его реализация выполняется отдельным следующим шагом.
- Если результат шага меняет предпосылки более поздних шагов, сначала обновляется этот план.
- Основанием для решений служат код, игровые данные и результаты моделирования, а не файл `README`.
- В пояснительном тексте используются русские термины; иностранное написание допустимо только для точных технических обозначений, оформленных обратными кавычками.

## Зафиксированные принципы

### Порядок проектирования

1. Зафиксировать текущее состояние и сделать воспроизводимое моделирование.
2. Спроектировать боевые роли.
3. Определить новую систему характеристик и справочную формулу мощи.
4. Определить развитие предметов и масштабируемые формулы умений.
5. Пересмотреть баланс существующих предметов и добавить новое снаряжение.
6. Проверить роли в бою и зафиксировать эталонные сборки.
7. Только после этого проектировать и настраивать походы относительно новых сборок.
8. После перевода походов отдельно переработать бои против окружения в аномалиях.

### Справочная мощь и действующий каталог

- Единственный сводный показатель персонажа называется «мощь» и служит только для наглядного показа постоянных личных характеристик. Он может отображаться в профиле, итогах боя и справочном личном рейтинге, но не является обещанием победы.
- Справочная мощь равна `Kмасш × Ж × У`, где `Ж` — живучесть, а `У` — обычный урон за единицу времени в нейтральной упрощённой модели. Постоянный коэффициент меняет только вид числа; после фиксации опорного полного комплекта он выбирается так, чтобы этот комплект имел мощь ровно 1000.
- Мощь не используется боем, созданием или настройкой противников, выбором сложности, подбором соперников, доступом к содержимому, наградами, ценами, выпадением предметов и иными игровыми решениями.
- `Ж × У` и линейная ценность характеристик разрешены в проверках только как вспомогательные признаки. Предметы и сборки принимаются по воспроизводимым боям и сохранению обещаний и слабостей проверочных полных сборок; ни одна такая оценка не хранится в игровых данных и не определяет цену.
- Формула и новый каталог проектируются сверху вниз. Характеристики и назначение любого существующего `ItemObject`, а независимо от них каталог и формулы модификаторов экземпляров разрешено менять; прежние числа не являются целью баланса.
- Существующие `code`, инвентари и связанные записи сохраняются либо переносятся безопасным явно утверждённым способом.
- Умения модификаторов получают реальные процентные или событийные формулы. Если модификатор постоянно меняет характеристику предбоевого снимка, это изменение естественно попадает в `Ж` или `У`; событийные и командные эффекты оцениваются проверочными боями без искусственной прибавки к мощности. Их ранг и действие показываются отдельно.
- Сила воздействия складывается по прозрачной общей формуле `L=min(100;80+Σlᵢ)`: основа 80 и предел 100 общие для всех, `lᵢ` суммируются со всех предметов и не связаны с ролью либо модификатором. Она управляет только использующими её боевыми механиками и не входит отдельным слагаемым в мощь.
- Версия формулы сохраняется у каждого умения в начальном снимке участника. Отсутствующее значение означает прежний вариант; после общей приёмки экипировка игрока использует новую версию во всех новых боях, а умения неигрового содержимого переходят на неё только после собственной настройки и проверки.
- Балансировочный предел грозового усиления не вводится; `+10` и `+20` остаются только контрольными ступенями расчётов.
- Для каждого безопасного базового числа `b` объекта характеристика на уровне `n` равна `B₁(b,n) = b + ⌊(n × b + 30) / 60⌋`. Прибавка перехода `Z₁(o,n)` — разность состояний `n + 1` и `n`, а не постоянное поле предмета. Состояния и переходы не записываются в `TOML` или БД. Ячейки влияют только на цену и проверочный ориентир; вторичные характеристики напрямую не растут. Формула не зависит от роли, модификатора, владельца, сборки или опорного окружения.
- Цена грозового усиления зависит только от уровня и постоянного коэффициента занятой ячейки. Различия предметов одной ячейки выравниваются их прибавками и проверками готовых сборок, а не разной ценой.
- Действующая кривая вероятностей успеха, обычной неудачи и отката грозового усиления сохраняется без изменений.

### Сложность и размер группы в обычных походах

- `D1–D10` — основная содержательная шкала развития.
- Вехи похода: `D1`, `D5` и `D10`; промежуточные уровни усиливают составы и характеристики.
- `D11+` — Восхождение: числовой рост и сочетания уже известных противников и умений.
- В интерфейсе сохраняется непрерывная нумерация `D1`, `D10`, `D11` и далее; `D11+` дополнительно помечаются как Восхождение.
- Общий рекомендуемый размер группы для походов: `R = 5`.
- Категории прохождения:
  - Вылазка: 1–3 игрока;
  - Поход: 4–7;
  - Дружина: 8+.
- Размер группы меняет масштаб испытания, но не открывает отдельные виды содержимого.
- Новый участник обычно должен немного помогать группе.
- Ни прежний `legacyPower()`, ни новая справочная мощь не участвуют в создании походов по новой модели.

### Выбор сложности и рекорд обычных походов

- В настройках группы администратор заранее выбирает постоянную стратегию:
  - 🟢 `SAFE` → `max(1, record - 1)`;
  - ⚔️ `CURRENT` → `max(1, record)`;
  - 🔥 `NEW_RECORD` → `record + 1`.
- Новый общий рекорд групп начинается с `D0`; старые результаты и сезонные очки не удаляются.
- Стратегия по умолчанию — `CURRENT`.
- Рекорд общий для всех типов походов.
- Для одинакового рекордного значения `D` дополнительно сохраняются минимальный состав, тип похода и дата достижения.

### Бои против окружения в аномалиях

- Безопасная аномалия и бой против окружения после неудачного поиска другой группы проектируются как отдельное содержимое, а не как ещё один потребитель мощности.
- Новый генератор получает явно сохранённые версию правил, режим, сложность, шаблон, размер группы и вариант либо начальное значение случайности. Он не получает персонажей, их экипировку или `power()`.
- Одинаковые версия, режим, сложность, шаблон, размер группы и вариант создают одинаковых противников. Рост экипировки повышает вероятность победы, но не характеристики этих противников.
- Источник и шкала сложности аномалий, составы четырёх шаблонов, целевые результаты для групп из 1–5 персонажей и награды утверждаются отдельно. Они не выводятся из мощности или рейтинга групповых боёв.
- Все параметры боя фиксируются до его начала и сохраняются для повторного чтения. Уже запущенные аномалии завершаются по прежней версии.
- Переработка боёв между группами не входит в эту ветку; меняется только их замена боем против окружения при неудачном поиске соперника.

### Награды обычных походов

- Базовая награда не уменьшается из-за размера группы.
- Вылазка получает дополнительную награду за сложное прохождение.
- Дружина не получает отрицательный множитель, а просто не получает дополнительную награду за сложное прохождение.
- Используются существующие награды: деньги, предметы и сезонные очки походов.
- Новая валюта в первую версию не добавляется.

## Важные факты текущей реализации

- `ItemObject` задаёт атаку, защиту, здоровье, шанс критического удара, уклонение, множитель критического урона, скорость, угрозу, дальность и одновременно занимаемые ячейки экипировки.
- `ItemRarity` сейчас даёт от 0 до 4 очков отдельно назначенного модификатора, умноженных на число занятых объектом ячеек; очки одинакового `ActiveEnum` суммируются по комплекту.
- Грозовое усиление масштабирует только атаку, защиту и здоровье; вторичные характеристики не меняются.
- Шесть наносящих урон умений задают исходный урон постоянными значениями 5–30 по рангу, а `SELF_HEAL` восстанавливает 10–30 здоровья. Эти величины не зависят от характеристик предметов или усиления, поэтому их относительный вклад падает по мере развития сборки. `BERSERK` уже является процентным исключением.
- Каталог загружается с обновлением существующих и добавлением отсутствующих записей по `ItemObject.code`. Изменение характеристик объекта с существующим `code` автоматически меняет все уже выпавшие экземпляры этого предмета.
- Удаление объекта из `TOML` не удаляет строку из БД и не исключает её из набора случайно выпадающих предметов.
- Поэтому до изменения каталога необходимо отдельно определить новую формулу мощи, модель характеристик предмета, чтение уже выданных экземпляров после изменения и способ вывода объектов из набора случайного выпадения.
- Текущее создание походов одновременно зависит от прежней формулы `power()` игроков, прежнего уровня похода и коэффициентов размера группы.
- Один генератор обслуживает безопасную аномалию и бой против окружения после неудачного поиска группы. Сейчас он берёт 92% суммы `BattlePersonage.power()` участников и двоичным поиском подгоняет характеристики противников; явные версия содержимого и сложность аномалии не сохраняются.
- Предварительная проверка миконидов показала скачки доли побед между `N=1/2` и около `N=7/8`, а также большой разрыв между случайными, базовыми и специализированными против конкретного соперника сборками.

## Пошаговый план

### [x] Шаг 0. Создать поддерживаемый план в формате `Markdown`

Создать этот документ, закрепить порядок последовательного выполнения и перенести проектирование боевых ролей и снаряжения перед балансом походов.

**Результат:** план создан; игровой код не изменялся.

### [x] Шаг 1. Полная проверка текущих экипировки, походов и боёв против окружения в аномалиях

Проверить по коду и данным:

- каталог `ItemObject` по ячейкам экипировки, видам атаки и защиты и диапазонам характеристик, а отдельно — каталог модификаторов;
- реальные правила назначения модификатора экземпляру, получения его очков из редкости, грозового усиления, генерации и сохранения предметов;
- все способы получения предметов и формирования набора случайного выпадения;
- исторические проверочные комплекты и перекосы сочетаний;
- все связи `raidLevel`, `RaidParams`, наград, очков сезона, интерфейса `Telegram`, таблиц групп и рейтингов;
- известные неточности прежней формулы `power()`, дальности, позиции и масштабирования по размеру группы.
- общий генератор двух разновидностей боя против окружения в аномалиях, его зависимость от `power()`, шаблоны, сохраняемые данные и отсутствие версии сложности.

**Итоговый материал:** рядом с планом появляется отдельный файл с таблицей исходного состояния каталога и картой зависимостей.
**Критерий завершения:** нет неизвестных путей, способных неожиданно изменить существующие предметы или развитие походов.

**Результат:** проверка завершена; подробное исходное состояние, карта зависимостей, проверки и риски вынесены в [отдельный отчёт](raid-redesign-step-1-audit.md). Игровой код не изменялся.

### [x] Шаг 2. Сделать воспроизводимый стенд моделирования боёв

Добавить управляемое начальное значение генератора случайных чисел для всех случайных решений боя, включая начальную инициативу, перемешивание, выбор цели, критический удар, уклонение, разброс урона и случайные варианты неигровых персонажей. Моделирование для настройки не должно запускаться в обычном наборе модульных проверок.

Стенд должен собирать:

- долю побед и доверительный интервал;
- медиану и `p95` длительности;
- выживших и остаток здоровья;
- нанесённый и полученный урон, число ходов;
- результаты по сборке, составу команды, сложности и размеру группы.

Для поиска параметров использовать 1 000 боёв на ячейку, для приёмки ключевых ячеек — 10 000.

**Итоговый материал:** воспроизводимый стенд с управляемой случайностью, ручной сценарий запуска, включаемый системным параметром, и отдельный отчёт о реализации и проверках.
**Критерий завершения:** одинаковое начальное значение генератора даёт одинаковый отчёт; обычная сборка `Maven` не замедляется.

**Результат:** критерий выполнен; генератор случайностей боя, создание исходных боевых составов и прежнее создание походов охвачены общей областью управляемой случайности, показатели и отчёт в формате `Markdown` воспроизводимы, а тяжёлый сценарий запуска исключён из обычного набора `Surefire`. Реализация, команды запуска, проверки и ограничения описаны в [отдельном отчёте](raid-redesign-step-2-simulator.md).

### [x] Шаг 3. Спроектировать боевые роли

Сначала описать игровые роли независимо от конкретных текущих предметов. Для каждой роли определить:

- назначение в группе;
- позицию и рабочую дальность;
- основные и вторичные характеристики;
- предпочтительные виды атаки и защиты;
- распределение угрозы;
- набор подходящих существующих умений;
- сильные стороны, слабости и способы противодействия;
- проверочные смешанные боевые образы и результаты, которые считаются провалом баланса из-за стирания слабостей.

Утверждены семь основных ролей: Страж, Громила, Застрельщик, Убийца, Дальнобоец, Разрушитель и Тактик.

Тип атаки и тип защиты рассматривать отдельно от роли. Стрелок и Чародей могут быть вариантами одной и той же роли, если различаются только типом урона; самостоятельными ролями они становятся лишь при разном поведении в бою.

Количество новых и изменяемых умений или предметов заранее не ограничивается. Можно менять существующие умения, характеристики `ItemObject` и общие правила назначения модификаторов по ячейке и `ModifierType`. Предметы не закрепляются за ролями и доступны любому игроку. Каждое изменение должно выполнять обещание проверяемой сборки, иметь явное противодействие и измеримый критерий проверки; старое умение предпочтительнее исправить, чем добавлять его смысловую копию.

**Итоговый материал:** утверждённая таблица боевых ролей.
**Критерий завершения:** у каждой роли есть отличимое назначение в бою, преимущество и уязвимость; пользователь утвердил список.

**Результат:** пользователь утвердил [отдельную таблицу боевых ролей](raid-redesign-step-3-archetypes.md) с семью основными ролями: Страж, Громила, Застрельщик, Убийца, Дальнобоец, Разрушитель и Тактик. Тип урона вынесен в отдельную ось; Стрелок и Чародей стали вариантами сборки. Приняты новые «Заслон», «Проникновение», «Накопление» и «Срыв темпа», а `HIT_AND_RUN` будет переработано в настоящее перемещение. Разрешены обоснованные изменения существующих умений, объектов предметов и общих правил назначения модификаторов; роль относится к поведению полной сборки, а не к предмету. Игровой код и игровые данные не изменялись.

### [x] Шаг 4. Определить справочную формулу мощи

Определить показатель «мощь» как произведение живучести `Ж` и урона за единицу времени `У` с отдельным положительным коэффициентом отображаемой шкалы. Корень и аддитивная сумма характеристик не используются.

Зафиксировать равномерную нейтральную смесь типов и символические правила здоровья, защиты, уклонения, скорости, критического удара и рабочей дистанции. Числовые параметры боевых правил, пределы, опорный комплект и коэффициент масштаба на этом шаге не выбираются. Дальность, угроза, перемещение, управление и событийные эффекты умений не получают выдуманного слагаемого: их ценность живёт только в испытаниях. Старые точные числа проверочных сборок признать недействительными и пересчитать вместе с каталогом.

Мощь является только справочным числом для показа игроку. Любая игровая система, включая походы и аномалии, обязана использовать собственные явно заданные параметры и не имеет права принимать решение по `power()`.

**Итоговый материал:** [отдельный результат по справочной формуле мощи](raid-redesign-step-4-stat-system.md).

**Критерий завершения:** пользователь утвердил определения `Ж` и `У`, равномерную нейтральную смесь, границу учитываемых механик, поздний выбор числовых параметров и исключительно справочное назначение мощности.

**Результат:** мощь равна `Kмасш × Ж × У`; на момент завершения этого шага изменяемые боевые величины оставались параметрами, а позднее шаг 6 сохранил общую матрицу защиты и зафиксировал `Bзащ=500` и `Iход=1000`. `Kмасш` будет выбран после фиксации опорного комплекта только ради удобной опорной отметки 1000. Аддитивные `Pбез`, `Цэф` и статическая ценность предмета удалены из решения. `Ж × У` разрешено использовать как вспомогательный признак в проверках, но баланс принимается по боям. Пользователь отдельно зафиксировал, что мощь нужна только для показа и не управляет игрой. Код и игровые данные не изменялись.

**Исследование:** [сравнение формул мощи в похожих системах](raid-redesign-power-formula-research.md) подтверждает `Ж × У` для нейтральной упрощённой дуэли и показывает принципиальную границу: событийные и командные умения нельзя честно свернуть в это число. Принято, что повышение ранга умения модификатора может усилить персонажа, но не изменить справочную мощь; ранг показывается отдельно.

### [x] Шаг 5. Определить развитие предметов и цену грозового усиления

Выбрать разделение развития между объектом каталога, редкостью и грозовым усилением. Устранить перекос, при котором предметы на вторичных характеристиках почти не растут, но не увеличивать напрямую опасные вероятности, скорость, дальность и управление.

Версия 1 применяет одну накопительную формулу отдельно к здоровью и каждому существующему числу атаки и защиты объекта. Для целых `b ≥ 0` и `n ≥ 0`:

`B₁(b,n) = b + округлить_половину_вверх(n × b / 60) = b + ⌊(n × b + 30) / 60⌋`.

Состояние объекта `Bрост,1(o,n)` состоит из результатов для всех его безопасных базовых чисел. Прибавка отдельного перехода вычисляется как `Z₁(o,n) = Bрост,1(o,n + 1) − Bрост,1(o,n)` и зависит от уровня. Каждый уровень считается непосредственно от `+0`, поэтому округление не накапливается, а повышение и понижение обратимы. Выполняются `B₁(b,0) = b`, `B₁(b,60) = 2b`, `B₁(b,n + 60) = B₁(b,n) + b` и ошибка не более половины единицы относительно `b × (1 + n / 60)`.

Тип, дальность и постоянное условие атаки, а также тип защиты только связывают рассчитанное число с существующей частью. Код предмета, переводы, ячейки, вторичные характеристики, владелец, другие предметы, редкость и модификатор экземпляра в формулу не входят. Скорость, критический удар, уклонение, дальность, угроза и сила воздействия остаются неизменными и влияют через настоящие боевые формулы. Отрицательные база или уровень, неположительное значение объявленной атаки или защиты, отсутствие положительной безопасной характеристики, повтор механического ключа и переполнение завершают расчёт явной ошибкой.

Для нового каталога здоровье равно нулю либо не меньше 60, а каждая объявленная атака и защита не меньше 60. Это гарантирует изменение каждой положительной безопасной характеристики на каждом успешном переходе. Прежние меньшие значения остаются читаемыми, но могут иметь переходы без изменения. Искусственный минимум `+1`, объединение характеристик в группы и распределение остатков не используются.

Состояния уровней и переходы являются производными значениями и не записываются в каталог или БД и не сохраняются как один постоянный набор. Для воспроизводимости фиксируются версии формулы и каталога, а автоматически созданная таблица результатов служит только проверочным отчётом. Роль относится только к наблюдаемому поведению полной сборки и не является полем либо ограничением предмета.

Задать постоянные коэффициенты ячеек для цены и проверочного ориентира: основная рука 4, вторая рука 1,25, корпус 2,5, ноги 1,75, обувь, голова и руки по 1, предмет для двух рук 5,25. В расчёте хранить их целыми четвертями `16 / 5 / 10 / 7 / 4 / 4 / 4`, сумма полного комплекта — 50. Цена попытки зависит только от коэффициента ячейки и уровня.

Задать идеальный диагностический рост полного комплекта кривой `G(n) = (1 + n / 60)³`, а одного перехода — `F(n) = [(n + 61) / (n + 60)]³`. Фактические переходы могут ступенчато отклоняться из-за целочисленного округления. Проверять отдельно прирост `Ж`, `У` и их произведения. Прежние автоматические 5% каждой основной характеристики за уровень не сохранять.

Сохранить действующую кривую вероятностей успеха, обычной неудачи и отката без изменений. `+0/+3/+6/+10/+20` остаются контрольными ступенями, но балансировочный предел уровня не вводится. Формулу цены зафиксировать как `max(1, округлить_половину_вверх[C₀ × μⁿ × qₛ / 4])`, где `μ = u/v` хранится точным отношением целых чисел; цену повышения редкости считать отдельно, а `C₀`, `μ` и экономический горизонт определить на шаге 18. На шаге 8 определить типы данных и параметрический расчёт отдельной технической границы; её окончательное значение подтвердить на шаге 18 после настройки цены.

**Итоговый материал:** [отдельный проект развития предметов и цены грозового усиления](raid-redesign-step-5-item-progression.md).

**Критерий завершения:** утверждены разделение развития объекта и модификатора экземпляра, точные функции состояния `B₁` и перехода `Z₁`, их входы, накопительное округление, ошибки и числовые границы, отсутствие хранения производных значений, требование к масштабу нового каталога, коэффициенты ячеек, проверочная кривая полного комплекта, сохранение действующих исходов попытки, формула цены, контрольные отметки и проверки высоких уровней.

**Результат:** каждая безопасная характеристика получает накопительную прибавку, округлённую от `n × b / 60`; переход `Z₁(o,n)` выводится из соседних состояний и может чередоваться между соседними целыми числами. Два объекта одной ячейки имеют одинаковую цену, но разные траектории при разных базах. Для нового каталога каждое безопасное число равно нулю либо не меньше 60, а у предмета есть хотя бы одно положительное такое число; действующий каталог это условие выполняет не полностью и будет переработан. Вторичные характеристики не переводятся в основные: их влияние, включая усиление суммарной атаки скоростью и критическим ударом, проверяется на готовых сборках. `G(n) = (1 + n / 60)³` остаётся идеальным ориентиром отчёта, а числовые границы боевой приёмки определяются вместе с новым каталогом и подтверждаются боями. Кривая исходов сохраняется, несмотря на нулевое ожидаемое изменение уже на `+8`; `C₀` и `μ` настраиваются с наградами на шаге 18. Код и игровые данные не изменялись.

### [x] Шаг 6. Определить формулы умений и правила измерения

Для каждого существующего и нового умения определить формулу эффекта на всех пяти рангах:

- основу масштабирования — атаку, здоровье либо силу воздействия и шкалу очерёдности;
- процентный коэффициент и обоснованный минимум;
- использование характеристик до или после временных изменений;
- порядок защиты, уклонения, критического удара, разброса, перемещения и реакций;
- правила округления, перезарядки и верхние пределы;
- воспроизводимые показатели боевого эффекта и способ их измерения только в проверках.

Каждое очко модификатора должно давать заметный монотонный результат. Постоянное число допустимо только как обоснованный минимум, доля здоровья цели — только с пределом, а дискретное перемещение не обязано становиться процентом. Число новых и изменяемых умений не ограничивается, но каждое должно выполнять обещание проверяемой сборки и иметь противодействие.

**Итоговый материал:** [утверждённые формулы умений и правила измерения](raid-redesign-step-6-skill-formulas.md).

**Критерий завершения:** утверждены переход от очков модификатора к реальному эффекту, формулы действующих и новых умений, порядок полного хода, пределы и способ будущей боевой проверки; умения не возвращают отдельный рейтинг мощности, а для шага 12 заданы критерии, подтверждающие, что их эффект не исчезает и не становится подавляющим по мере развития сборки.

**Результат:** зафиксированы точные значения 1–8 эффективных очков, процентные формулы одиннадцати действующих и четырёх новых умений, вычисляемый основной тип атаки, сила воздействия `L=min(100;80+Σlᵢ)`, части атаки по дистанции, единая обработка смешанного урона и полный порядок реакций. Для всех пятнадцати умений заданы назначение, противодействие, измеритель и собственный предел; общий искусственный предел доли урона удалён. Определены точные и статистические проверки, независимые последовательности случайности и версия формулы каждого умения: после общей приёмки принятая версия каталога становится действующей, экипировка игрока получает новую версию умений во всех новых боях, а прежние неигровые умения сохраняются до собственного переключения. Шаг 12 подтверждает решение на новом каталоге либо явно возвращает его на пересмотр; код и игровые данные не изменялись.

### [ ] Шаг 7. Спроектировать новый каталог снаряжения

Подготовить два раздельных материала: каталог `ItemObject` по ячейкам и собственным боевым признакам и внешнюю карту полных сборок с описанием их наблюдаемого ролевого поведения. Предметы не закрепляются за ролями.

- какое место в новом каталоге получает каждый существующий `ItemObject.code` и какие характеристики или занятые ячейки заменяют прежние;
- какие предметы случайно уже соответствуют новой цели и потому действительно могут сохраниться без изменений;
- какие новые `ItemObject` добавляются;
- какие предметы становятся равноценной альтернативой, улучшением или специализированным выбором против конкретного соперника;
- какие старые предметы перестают выпадать, но сохраняются у владельцев;
- нужны ли новые модификаторы, в каких ячейках они участвуют в общем отборе и какой `ModifierType` им назначен.

Правило назначения модификатора остаётся общим: его ячейки и `ModifierType` сопоставляются с одной переданной ячейкой и наличием атаки или защиты у объекта. При случайной выдаче передаётся запрошенная ячейка, при первом повышении редкости — первая занятая объектом ячейка по `PersonageSlot.id`; для объекта на несколько ячеек полный набор новых назначений модификаторов является объединением результатов этих путей. Ранее сохранённый экземпляр может содержать историческое назначение вне нынешнего отбора; оно отдельно проверяется при переносе данных. Если существующих признаков недостаточно, изменение общего правила проектируется явно; отдельный список разрешённых предметов не вводится. Во внешней карте сборок отдельно показать физические и магические варианты Дальнобойца и Разрушителя, не превращая тип урона в самостоятельную роль.

Для каждого модификатора отдельно указать `ActiveEnum`, `ModifierType`, доступные ячейки, ссылку на уже утверждённую формулу умения и ожидаемые результаты проверочных боёв на пяти контрольных уровнях грозового усиления: `+0/+3/+6/+10/+20`. Проверки охватывают все объекты, которым общие правила действительно могут назначить этот модификатор. Справочная формула мощи, схема развития и формулы умений повторно не выбираются; их изменение требует явного возврата к соответствующему решению.

Для каждого физически достижимого сочетания двух разных модификаторов до итоговых прогонов записать ожидаемый знак и опасную границу взаимодействия `Sab` в естественной основной величине обеих механик. Границу нельзя подбирать после просмотра результата; универсального числа для разных механик нет.

Отдельно сопоставить предметы для двух рук и физически допустимые пары одноручных предметов без модификаторов, с одинаковым `ActiveEnum` и одинаковой суммой очков и с двумя разными модификаторами пары. Преимущество двух разных умений выявляется боями и регулируется их формулами, общим правилом назначения модификаторов или ценой редкости; искусственно добавлять его к `power()` нельзя.

Для каждого объекта заранее определить `code`, одновременно занимаемые ячейки, переводы, точные базовые характеристики, список частей атаки с `dmin/dmax`, защиту, дальность и неотрицательный вклад силы воздействия `lᵢ`. Прежняя неделимая атака переносится как одна часть с `dmin=1`; дальняя условная часть хранится отдельно, чтобы сближение выключало только 25–35%, а не всю атаку. Сила воздействия полной экипировки всегда вычисляется общей функцией `L=min(100;80+Σlᵢ)`; предмет не получает роль, требование конкретного модификатора или ручной основной тип `t₀`. Внешний отчёт только показывает вычисленный `t₀` и отсутствие регулярно пропадающих за пределом вкладов `lᵢ`.

Здоровье должно быть равно нулю либо не меньше 60, каждая объявленная атака и защита — не меньше 60, а у объекта должна существовать хотя бы одна положительная безопасная характеристика. Полные проверочные сборки являются внешними данными проверки, а не полями объекта. Использовать утверждённую на шаге 5 кривую желаемого роста; предложить кандидаты опорной сборки и пределов наблюдаемого поведения. Эти кандидаты нужны для испытаний и окончательно принимаются на шаге 12. Существующие значения `code` не переименовывать без отдельной причины для переноса данных.

Применить неизменные функции `Bᵥ` и `Zᵥ` ко всем объектам и автоматически создать таблицу «базовые числа и уровень → состояние → прибавка следующего перехода». Расчётный отчёт должен охватить состояния `+0…+21` и каждый переход `0…20`. Затем подготовить полную входную матрицу будущих боевых прогонов: физические комплекты, пары предметов для рук, типы входящего урона, условия атаки, отметки `+0/+3/+6/+10/+20`, новые назначения общего отбора и исторически сохранённые назначения. Для каждой строки записываются ожидаемое поведение, основная величина, предел и допустимая область взаимодействия, но фактический прогон новых формул выполняется после реализации на шаге 12. Таблица и ожидаемые результаты не являются игровыми настройками. Ручная правка отдельного перехода запрещена: при локальном провале меняются базовые характеристики `ItemObject`, при системном — шаг 5 и общая формула.

**Критерий завершения:** каталог объектов, отдельный каталог модификаторов и общие правила их назначения, все базовые характеристики, вычисленные состояния и переходы, внешние проверочные сборки, ожидаемые результаты и полная входная матрица прогонов приняты до изменения игровых `TOML`, кода или БД.

### [ ] Шаг 8. Реализовать справочную мощь и развитие предметов

Реализовать общую основу решений шагов 4–6 без одновременной замены всего каталога и боевых умений:

- добавить список частей атаки с точными условиями `dmin/dmax`, вклад силы воздействия `lᵢ` с безопасным значением 0 для прежних данных и общую функцию `L=min(100;80+Σlᵢ)`; поле `Z` не добавлять;
- реализовать чистую функцию `Bᵥ(b,n) = b + ⌊(n × b + 30) / 60⌋` и производную функцию перехода `Zᵥ(o,n)`; каждое состояние вычислять по запросу непосредственно из базового `ItemObject` и уровня, не хранить производные значения и не сохранять заранее один постоянный набор; ячейки, вторичные характеристики, сохранённые данные и экипировка владельца результат не меняют;
- реализовать расчёт справочной мощности `Kмасш × Ж × У` отдельным вычислителем над постоянными характеристиками, не помещая новый метод в боевой объект; вынести общие `W`, `Bзащ=500` и `I=1000` в один источник для боя и мощности; также реализовать применение `Bᵥ` к здоровью и каждой части атаки и защиты и цену грозового усиления по коэффициенту ячейки; действующий расчёт вероятностей не менять, окончательное значение `Kмасш` и переключение отображения не выполнять до шага 12;
- запретить прямой рост вторичных характеристик и дополнительное удвоение цены предмета для двух рук;
- выполнять списание осколков и изменение уровня неделимо, отвергать повторную команду с устаревшим уровнем, вычислять цену из точного рационального `μ` без промежуточного округления и реализовать параметрический расчёт технической границы `Nтех`;
- отделить прежний расчёт как `legacyPower()` для прежних генераторов походов, чтения событий `LEGACY_POWER` и аномалии; до их переноса эти системы вызывают только явно названный прежний расчёт, а новые зависимости и от него, и от справочной мощности запрещены;
- покрыть единичными проверками `Ж`, `У`, независимость отношений мощностей от `Kмасш`, точные определения `Bᵥ` и `Zᵥ`, одинаковый результат для одинаковых входов, `b = 0`, прежние значения меньше 60, границу `b = 60`, точную половину, независимое округление частей, равенства `B(b,0) = b`, `B(b,60) = 2b`, `B(b,n+60) = B(b,n)+b`, телескопическую сумму переходов, обратимость уровня, ошибки и переполнение, коэффициенты ячеек, округление цены, неизменность действующих вероятностей, ожидаемое число попыток, повторную команду, неделимость операции и высокие уровни; проверочную ценность оставить только в исходниках проверок.

На этом шаге каталог, боевые умения, генераторы походов и награды не перенастраиваются.

**Критерий завершения:** обе формулы разделены, новая мощь и развитие вычисляются точно на новых и прежних проверочных данных, прежние данные по-прежнему читаются, список временных потребителей `legacyPower()` зафиксирован, а проверка зависимостей запрещает обращение к новой мощности из игровых механизмов; полная сборка `Maven` проходит.

### [ ] Шаг 9. Реализовать масштабируемые умения и порядок боя

Реализовать решение шага 6 на проверочных предметах и составах, ещё не переключая основной каталог:

- заменить утверждённые постоянные величины урона и лечения процентными формулами и добавить новые умения;
- передавать в умение исходный вектор атаки, фактическую дистанцию, окончательную цель и попадание без повторного вычисления; `t₀` выводить из экипировки чистой функцией;
- реализовать точную целочисленную арифметику, общий порядок хода, смешанный урон одним событием, защиту, единое округление, симметричный разброс, перемещения, заряды, перехваты и личные либо общие перезарядки;
- ввести независимые именованные последовательности случайности для парных проверок;
- отделить боевое умение от прежней оценки `skillPowerRating`: сохранить старые оценочные классы только за `legacyPower()` до удаления последнего прежнего потребителя, а новые умения не заставлять возвращать рейтинг мощности;
- сохранять версию формулы у каждого умения в снимке участника и сопоставлять по паре «`ActiveEnum` — версия»: отсутствие значения означает `LEGACY_SKILLS_V1`, проверочные умения — `SCALING_SKILLS_V1`; смешанный бой поддерживает обе версии, а четыре новых умения никогда не попадают в прежний сопоставитель;
- расширить отчёт моделирования уроном, лечением, перехватами, перемещениями, разрядами, задержками, выбором целей и изменениями угрозы по источникам.

Проверки охватывают 0–9 очков, округление, нулевую основу, смешанные типы, все ветви порядка, пределы, характеристики до и после временных изменений, полностью прежний бой и смешанный бой новых умений игроков с прежними умениями противника. Шаг 12 подтверждает зафиксированные коэффициенты на новом каталоге; провал возвращает решение на шаг 6 и создаёт новую версию.

**Критерий завершения:** все утверждённые формулы и ветви порядка хода покрыты воспроизводимыми проверками, игровой расчёт совпадает с описанием, вторичный урон не создаёт цепочек, полная сборка `Maven` проходит.

### [ ] Шаг 10. Реализовать новый каталог и показ игроку

Перенести утверждённые материалы шага 7 в отдельную ещё не действующую версию игровых данных: обновить текущие и добавить новые `ItemObject`, записать их базовые характеристики и реальные условия атаки; отдельно обновить каталог модификаторов и общие правила их назначения, добавить переводы. До приёмки шага 12 эта версия доступна только проверочному пути, а новые модификаторы не выпадают игрокам. Производные состояния и переходы, проверочную ценность, долю дальности, ролевые метки и оценочные коэффициенты в `ItemObject` не переносить. Версии формулы и каталога фиксируются на уровне выпуска. Существующие значения `code` сохранять либо переносить только по заранее утверждённому правилу; физически не удалять связанные строки.

Подключить к предметам формулы шагов 8 и 9. Подготовить профиль, итоги боя и справочный личный рейтинг к единой подписи «Мощь» и параметрическому `Kмасш`, не закрепляя окончательную шкалу до шага 12. Рядом явно пояснить приблизительный характер числа и отдельно показывать действие и ранг умения модификатора, точные изменения следующего перехода и его цену. Цена повышения редкости остаётся отдельным расчётом, потому что увеличивает очки модификатора, а не характеристики объекта.

На этом шаге генераторы походов и награды не меняются. Безопасность уже выданных экземпляров и фактического набора добычи подтверждается отдельно на шаге 11.

**Критерий завершения:** каталог загружается, во всех ячейках есть предметные объекты, а общее правило назначения модификаторов даёт непустой набор там, где он требуется; существующие `code` разрешаются, формулы боя и справочной мощности согласованы с данными, места показа готовы принять окончательный `Kмасш` без изменения формулы, полная сборка `Maven` проходит.

### [ ] Шаг 11. Реализовать безопасное обновление данных и жизненный цикл добычи

Проверить на `PostgreSQL` поведение существующих инвентарей после обновления характеристик с добавлением отсутствующих записей. Если предметы выводятся из выпадения, добавить явный признак доступности, например `lootability` или `enabled`, и фильтрацию `getRandomObject`, не удаляя строки с внешними ключами.

Проверить:

- существующие экипированные и лежащие в сумке экземпляры;
- новые выпадения;
- работа каждого исторически сохранённого назначения модификатора, включая пары вне нового отбора, и отдельно — общего правила новых назначений после обновления объекта;
- набор случайно выпадающих предметов;
- грозовое усиление до и после изменения баланса;
- возможность отката и чтение прежних данных.

Уровень уже выданного экземпляра сохраняется, а характеристики после обновления вычисляются как `Bᵥ(b,n)` от новой базы каталога. Отдельно сравнить старую формулу 5% и новую траекторию на всех реально сохранённых уровнях, определить и утвердить политику ретроактивного изменения или компенсации. Откат `n → n−1` вычисляет оба состояния непосредственно от `+0`, а не вычитает постоянную прибавку. Прежние базовые числа меньше 60 обязаны оставаться читаемыми даже при наличии переходов без изменения характеристики.

**Критерий завершения:** ни один существующий предмет и его уровень не теряются, последствия новой формулы для уже усиленных экземпляров явно приняты и проверены, откат обратим, прежние данные читаются, а выведенные предметы больше не выпадают.

### [ ] Шаг 12. Проверить боевые роли и зафиксировать эталонные составы для походов

Прогнать таблицы зеркальных боёв, противодействующих сборок и смешанных команд для утверждённых ролей полных сборок. Проверить одиночные и командные составы, позиции, дальность, угрозу, избыточность и сочетания умений. Сравнить экземпляры одного `ItemObject` на одном уровне без модификатора и с каждым назначаемым модификатором при одинаковых вычисленных характеристиках, затем проверить реальные каталожные сборки. Измерять урон, лечение, управление, число действий, выживаемость и долю побед; не сворачивать результат в поправку к справочной мощности. Этот шаг подтверждает зафиксированную версию формул, а не подбирает коэффициенты во время прогона: провал предмета возвращается на шаг 7, провал формулы умения — на шаг 6, системный провал развития — на шаг 5.

Базовые критерии:

- зеркальный бой близок к 50%;
- ожидаемая противодействующая сборка даёт заметное, но не гарантированное преимущество;
- нет одной сборки, доминирующей во всех противостояниях;
- каждая роль сохраняет заявленную слабость;
- несколько хороших улучшений дают ощутимый прирост, но не заменяют продуманный состав;
- доля урона, лечения или управления от каждого ключевого умения остаётся заметной, но не подавляющей на контрольных ступенях `+0/+3/+6/+10/+20`; повышение ранга даёт ожидаемый прирост;
- предмет для двух рук и пара одноручных предметов сравнимы при одинаковых основных характеристиках, уровне, общей цене усиления и редкости; систематическое превосходство исправляется формулами умений модификаторов, общими правилами их назначения или ценой редкости.

После приёмки создать зафиксированные начальные, средние и сильные команды для настройки походов. Затем выбрать опорный полный комплект, вычислить для него окончательный `Kмасш` со справочной отметкой ровно 1000, сохранить коэффициент как версионированную константу и переключить профиль, итоги боя и справочный рейтинг на новую шкалу. Выбор масштаба не является частью боевой настройки. Изменение правил формулы требует явной новой версии отображения; изменение предметов эталонных команд требует повторного прогона боёв независимо от мощности.

Одновременно после приёмки сделать принятую версию каталога действующей. Новые базовые характеристики и части атаки применяются ко всем существующим и новым экземплярам по их `ItemObject.code`; принятые доступность добычи, каталог модификаторов и общий отбор используются для новых выдач, включая выпадение четырёх новых модификаторов. Политика изменения уже усиленных экземпляров и возможность отката к этому моменту должны быть приняты и проверены на шаге 11. Включить `SCALING_SKILLS_V1` для умений из экипировки игроков во всех вновь создаваемых боях, включая бои игроков и мировые походы. Уже созданные снимки без версии продолжают использовать `LEGACY_SKILLS_V1`. Умения неигровых персонажей сохраняют версию своего содержимого и переходят отдельно на шагах переключения миконидов, Волчьей стаи, Орды зомби, Стаи маггусей и аномалий.

Повторно вычислить чистыми функциями реализованной версии все состояния `+0…+21` и переходы `0…20` и подтвердить совпадение с автоматическим отчётом шага 7. На контрольных отметках проверить все физические комплекты сначала без модификаторов, затем со всеми новыми и историческими назначениями. Нарушение отдельного объекта возвращает его базовые характеристики на шаг 7 без ручной подмены результата; системное нарушение возвращает формулу на шаг 5; нарушение только с модификатором возвращает формулу умения или общий отбор. После приёмки зафиксировать номера версий формулы и каталога. Новый объект не меняет траектории старых объектов; изменение базовых полей старого объекта или версии формулы является явным изменением баланса выпуска.

**Критерий завершения:** баланс проверочных боевых образов принят пользователем, вычисленные чистыми функциями состояния и переходы прошли проверки, пара версий формулы и каталога зафиксирована и принятая версия каталога включена, эталонные составы и опорный комплект утверждены, `Kмасш` вычислен и версионирован, места показа используют окончательную шкалу, а ни один игровой механизм её не читает.

### [ ] Шаг 13. Спроектировать миконидов `D1–D10` под новые роли

Подготовить точную таблицу составов, характеристик и умений:

- `D1`: базовые Страж и Пращник;
- `D2–D4`: рост чисел и масштаба состава;
- `D5`: усиленные варианты на существующих умениях;
- `D6–D9`: рост доли усиленных противников и тяжёлые сочетания;
- `D10`: полная версия испытания с главарём и всей палитрой миконидов.

Испытание должно проверять несколько ролей, но не требовать единственную обязательную сборку. Пращник должен иметь дальность, соответствующую `BACK`, без скрытого автоматического переноса вперёд.

**Критерий завершения:** полная таблица `D1–D10` утверждена до написания нового генератора.

### [ ] Шаг 14. Реализовать миконидов с фиксированной сложностью вне основного игрового пути

Ввести запрос генерации вида `RaidEncounterRequest(raidType, difficulty, partySize)`. Новый генератор принимает только сложность и размер группы — не список персонажей и не их `power()`.

Прежний генератор сохранить для уже созданных событий. Ввести версии `LEGACY_POWER` и `FIXED_DIFFICULTY_V1`; только первый путь временно использует `legacyPower()`, а не новую мощь.

**Критерий завершения:** изменение экипировки при одинаковых сложности и `N` создаёт идентичный состав противников; основной игровой путь пока использует прежнюю версию.

### [ ] Шаг 15. Настроить масштабирование по размеру группы

Подобрать состав и характеристики противников моделированием для `N=1–30`. Основные контрольные размеры — 3, 7 и 15 игроков.

Целевые показатели около предела доступной сложности:

- `N=3`: около 55% побед;
- `N=7`: около 60%;
- `N=15`: около 65%;
- допуск ±5 процентных пунктов.

Между соседними значениями `N` не должно быть необъяснимых скачков более 10 пунктов. Новый типичный участник не должен снижать долю побед группы.

**Критерий завершения:** кривая угрозы по результатам боёв и отчёт для `N=1–30` утверждены пользователем.

### [ ] Шаг 16. Настроить Восхождение `D11+`

Использовать полную палитру `D10`, увеличивая числовые характеристики и смешивая уже известные сочетания усиленных противников и главарей. Новых типов врагов после `D10` не вводить.

Кривая должна быть монотонной, не переполнять числовые типы на высоких значениях `D` и не иметь игрового предела; допустима только техническая защита от переполнения.

**Критерий завершения:** формула Восхождения и результаты моделирования `D10–D30` утверждены.

### [ ] Шаг 17. Реализовать рекорд и выбор сложности

Добавить отдельное хранилище нового развития, не переиспользуя прежнее поле `pgroup.raid_level`, пока остальные походы не переведены на новую модель.

Хранить лучшее общее значение `D`, минимальный состав, тип похода, дату и выбранную стратегию. Добавить в настройки группы три кнопки, доступные только администратору. При создании события вычислять `D` из стратегии и фиксировать его в `RaidParams`.

Старые события в формате `JSON` читать с учётом версии и запасного способа разбора как `LEGACY_POWER`. Новый рекорд записывать только после победы; победа на том же значении `D` меньшим составом обновляет почётный рекорд.

**Критерий завершения:** перенос данных создаёт `D0` без удаления истории; проверки покрывают стратегии, права и чтение прежних данных.

### [ ] Шаг 18. Спроектировать систему наград относительно новой экипировки

Только после фиксации ступеней характеристик и шкалы сложности подготовить точные формулы:

- базовая денежная награда по `D`;
- распределение редкости и уровней грозового усиления предметов по `D1–D10`;
- явные цены покупки и продажи предметов с учётом ячейки, редкости, источника и случайности получения, без рабочей величины ценности характеристик;
- цену повышения редкости, включая различие между одним модификатором предмета для двух рук с удвоенными очками и двумя независимо назначенными модификаторами пары;
- медленное улучшение набора наград во время Восхождения;
- сезонные очки походов;
- дополнительная награда за сложность Вылазки.

Вместе с доходом осколков окончательно настроить базовый тариф `C₀` и множитель удорожания `μ` грозового усиления, сохранив действующие вероятности исходов. Рассчитывать полную ожидаемую стоимость с учётом повторного подъёма после отката и округления каждой попытки, а не как сумму цен, поделённых на вероятность успеха. Отдельно сравнить пару одноручных предметов и двуручный предмет не только по среднему итогу, но и по размеру одного платежа, разбросу расходов и времени получения частичной пользы. Зафиксировать экономический горизонт `Nэкон`: уровень, до которого обычный путь развития проверяется в днях и числе доступных событий, но который не является пределом усиления. После выбора `C₀` и `μ` вычислить окончательную техническую границу `Nтех`, утвердить числовой запас `Rтех = Nтех − Nэкон` на весь расчётный срок штатной игры и проверить поведение на краю поддерживаемой области. Распределение серий исходов документируется как следствие неизменяемой кривой, но не перенастраивается.

Базовая денежная награда и базовая вероятность выпадения предмета не уменьшаются с размером группы. Защита от долгой серии неудач сохраняется. Дружина не получает отрицательный множитель.

**Критерий завершения:** утверждены прогноз наград для начальных, средних и сильных составов при `N=3/5/7/15` и `D1/D5/D10/D15`, цены покупки, продажи и повышения редкости, `C₀`, `μ`, `Nэкон`, окончательная `Nтех` и числовой запас `Rтех`; ожидаемая стоимость рассчитана на действующей кривой исходов.

### [ ] Шаг 19. Реализовать награды

Разделить расчёты базовой и дополнительной наград. Категорию прохождения и добавочную награду сохранять в результате события и показывать в отчёте. Персонажи со статусом `Exhausted` по-прежнему не получают награду.

Применить утверждённые `C₀` и точный рациональный `μ` грозового усиления, окончательное значение `Nтех`, цены покупки, продажи и повышения редкости. Автоматические проверки должны воспроизводить ожидаемую стоимость пути до контрольных уровней и `Nэкон` с учётом округления, обычных неудач, откатов и повторного подъёма.

**Критерий завершения:** проверки подтверждают отсутствие штрафа за размер группы в базовой награде, правильный расчёт дополнительной награды за сложность, ожидаемое развитие предметов, окончательные цены и параметры грозового усиления, безопасную техническую границу и правильное начисление очков походов.

### [ ] Шаг 20. Переключить новые события миконидов на основную модель

Новые события миконидов получают `FIXED_DIFFICULTY_V1`; уже созданные прежние события завершаются прежним генератором. Новые события миконидов обновляют рекорд сложности, но не прежний `MMR`.

Обновить переводы сложности, Восхождения и категорий «Вылазка», «Поход» и «Дружина».

**Критерий завершения:** полная таблица моделирования и команда `mvn -f game/pom.xml clean package` завершаются успешно; в новом пути нет чтения `power()` игроков.

### [ ] Шаг 21. Спроектировать и утвердить Волчью стаю `D1–D10`

Создать таблицу вех под зафиксированные роли и общую шкалу сложности.

### [ ] Шаг 22. Реализовать и переключить Волчью стаю

Повторить создание генератора с фиксированной сложностью, настройку для `N=1–30`, Восхождение и проверки чтения прежних событий.

### [ ] Шаг 23. Спроектировать и утвердить Орду зомби `D1–D10`

Определить составы обычных зомби, усиленных противников и главарей с существующими умениями и подходящим набором видов атак.

### [ ] Шаг 24. Реализовать и переключить Орду зомби

Повторить настройку и переход на основную модель без зависимости от мощи игроков.

### [ ] Шаг 25. Спроектировать и утвердить Стаю маггусей `D1–D10`

Определить составы таранщиков, маггусей, усиленных противников и главарей с правильными ролями по расположению.

### [ ] Шаг 26. Реализовать и переключить Стаю маггусей

Повторить настройку, Восхождение и переход на основную модель.

### [ ] Шаг 27. Удалить прежнее масштабирование походов

Удалить подбор неигровых персонажей двоичным поиском под суммарное значение `legacyPower()` игроков, `powerBonus`, настроечные поправки и обновление `MMR` на `+1/-2`. Удалить прежнее поле `pgroup.raid_level` только после подтверждения отсутствия активных событий прежней версии.

Перевести рейтинг уровня похода на общий рекорд сложности; сезонные очки походов оставить отдельным рейтингом.

### [ ] Шаг 28. Спроектировать бои против окружения в аномалиях

До изменения кода отдельно определить назначение безопасной аномалии и боя, заменяющего встречу групп после неудачного поиска. Для каждого из четырёх шаблонов и размера группы `N=1–5` подготовить роли, состав, расположение, характеристики и умения противников. Утвердить:

- отдельную шкалу или именованные варианты сложности, не зависящие от мощности игроков;
- способ выбора сложности в обоих режимах;
- начальные, средние, сильные и противодействующие эталонные команды;
- целевые коридоры доли побед, длительности, выживших, урона, лечения и управления;
- правила снятия грозового усиления в аномалии;
- связь режима и явной сложности с деньгами, осколками и предметами; рейтинг боёв между группами результатом такого боя не меняется.

Испытание не должно требовать одну обязательную роль. Переработка боя между двумя найденными группами остаётся за пределами шага.

**Итоговый материал:** отдельная утверждённая таблица содержимого и критериев для всех сочетаний шаблона и размера группы.
**Критерий завершения:** назначение двух боёв, выбор сложности, все составы и измеримые границы утверждены до реализации генератора.

### [ ] Шаг 29. Ввести версию и сохраняемые параметры аномалии

Добавить версию правил создания боя. Существующие записи без версии читать как прежний вариант `LEGACY_POWER_V1`; новые записи смогут использовать `FIXED_ENCOUNTER_V1`.

Для нового варианта до начала боя сохранять режим, явную сложность, шаблон, размер группы и вариант либо начальное значение случайности. Отдельно проверить сохранение времени начала поиска и поведение при изменении настроек уже запущенной аномалии. Перенос базы данных должен сохранять чтение всех состояний безопасной и опасной аномалии.

**Критерий завершения:** старые записи читаются без изменения поведения, а новый набор полей однозначно воспроизводит замысел будущего боя.

### [ ] Шаг 30. Реализовать новый генератор и моделирование вне основного пути

Ввести запрос создания боя вида `AnomalyPveEncounterRequest(version, mode, difficulty, template, partySize, variantOrSeed)`. Генератор не принимает персонажей, экипировку, рейтинг или `power()` и не подгоняет характеристики двоичным поиском.

Прежний генератор оставить без изменения для `LEGACY_POWER_V1`. Расширить стенд шага 2 матрицей `N=1–5 × четыре шаблона × эталонные команды`, включая противодействующие составы, и сохранять воспроизводимый отчёт рядом с планом.

**Критерий завершения:** одинаковый запрос создаёт одинаковых противников при любой экипировке и порядке участников; новый путь пока не используется запущенными игровыми событиями.

### [ ] Шаг 31. Настроить бои и награды аномалий

На новом генераторе подобрать утверждённые составы и характеристики по матрице шага 28. Отдельно проверить все четыре шаблона, размеры группы `N=1–5`, начальные, средние, сильные и противодействующие команды, а также оба режима использования боя против окружения.

Настроить награды по режиму и явной сложности без обращения к мощности. Проверить ожидаемый доход, неизменность рейтинга боёв между группами, единовременное завершение события и защиту от повторной выдачи наград.

**Критерий завершения:** все ячейки матрицы входят в утверждённые коридоры, рост экипировки повышает долю побед без изменения противников, доход не создаёт выгодного проигрыша или бесконечного повторения, а рейтинг боёв между группами не меняется.

### [ ] Шаг 32. Переключить новые бои против окружения в аномалиях

Новые безопасные аномалии и новые поиски соперника в опасных аномалиях получают `FIXED_ENCOUNTER_V1`. При закрытии состава либо начале поиска фиксируются все параметры будущего боя. Уже запущенные состояния прежней версии завершаются через прежний генератор; бой между найденными группами сохраняет собственные правила и рейтинг.

Проверить все состояния безопасной и опасной аномалии, повторный запуск после изменения настроек, чтение прежних журналов и отсутствие обращения к справочной мощности в новом пути.

**Критерий завершения:** оба новых пути боя против окружения работают по сохранённому сценарию, а активные и исторические события прежней версии остаются читаемыми.

### [ ] Шаг 33. Удалить прежнее масштабирование аномалий

После подтверждения отсутствия активных событий `LEGACY_POWER_V1` удалить прежний генератор, коэффициент `SAFE_POWER_RATIO`, двоичный поиск множителя и последний игровой вызов `legacyPower()`. Затем удалить сам прежний расчёт и проверки его временных потребителей.

Закрепить архитектурной проверкой, что справочная мощь используется только для показа в профиле, итогах боя и личном рейтинге. Создание боя, сложность, подбор соперника, награды, цены, добыча и доступ к содержимому не должны импортировать или вызывать её расчёт.

**Критерий завершения:** `legacyPower()` отсутствует, новый расчёт мощности не читается игровыми механизмами, обе разновидности боя против окружения проходят проверки, полная сборка `Maven` завершается успешно.

## Общие приёмочные критерии

- Боевые роли и предметы утверждены до начала настройки походов.
- Новая экипировка образует равноценные альтернативы и осмысленные улучшения, а не одну лучшую комбинацию.
- При одинаковых сложности и `N` экипировка игроков не меняет сгенерированных неигровых персонажей.
- Улучшение экипировки повышает долю побед и доступный предел сложности.
- Сложность монотонно повышает угрозу.
- Все размеры `N=1–30` получают один и тот же набор испытаний соответствующего `D`.
- Добавление игрока обычно помогает, но большая группа не получает существенного прироста доступной сложности только за счёт численности.
- Базовая награда не штрафует за друзей; малая группа получает дополнительную награду за сложность.
- Старые предметы, события и исторические отчёты остаются читаемыми после переноса данных.
- Справочная мощь показывается игроку, но не влияет ни на одно игровое решение.
- Одинаковый сохранённый сценарий аномалии создаёт одинаковых противников независимо от участников и их экипировки.
- Безопасная аномалия и замена группового боя используют версионированное содержимое и собственную явную сложность.
- После каждого шага реализации выполняется полная сборка `Maven`.

## За пределами первого сквозного этапа

- Новая универсальная основа сценарных механик боя.
- Полная переработка `FRONT`/`MID`/`BACK`.
- Новая валюта.
- Полная переработка мировых походов.
- Переработка боя между группами в опасной аномалии, кроме его замены боем против окружения при неудачном поиске.
